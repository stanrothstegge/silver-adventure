package main.java.utils;

/**
 * Holds DataTypes
 */
public enum DataType {
    /**
     * data types
     * INTEGER: 'in';
     * DOUBLE: 'do';
     * STRING: 'st';
     * CHAR: 'ch';
     * BOOLEAN: 'bo';
     * TRUE: 'tr';
     */
    BOOLEAN, INTEGER, DOUBLE, STRING, CHAR, TRUE, FALSE, VOID
}
